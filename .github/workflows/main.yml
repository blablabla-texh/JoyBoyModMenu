name: Build Tweak
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential curl perl git libssl-dev xz-utils

      - name: Setup Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=~/theos" >> $GITHUB_ENV
          echo "~/theos/bin" >> $GITHUB_PATH

      - name: Setup SDK (New Source)
        run: |
          mkdir -p ~/theos/sdks
          # On utilise une source alternative plus fiable
          curl -L "https://github.com/theos/sdks/raw/master/iPhoneOS13.0.sdk.tar.xz" -o sdk.tar.xz
          # Verification : si le fichier est minuscule, on utilise un autre lien direct
          if [ $(stat -c%s sdk.tar.xz) -lt 1000000 ]; then
            curl -L "https://bit.ly/sdks13" -o sdk.tar.xz
          fi
          tar -xJf sdk.tar.xz -C ~/theos/sdks/

      - name: Build Package
        run: |
          export THEOS=~/theos
          # On compile !
          make package FINALPACKAGE=1
        env:
          THEOS: ~/theos

      - name: Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: JoyBoyModMenu-Package
          path: ./*.deb
