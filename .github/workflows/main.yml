name: Build Tweak
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4

      - name: Setup Theos & SDK
        run: |
          # Installation propre de Theos
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          
          # Téléchargement manuel du SDK (Lien direct alternatif)
          mkdir -p $HOME/theos/sdks
          curl -L https://github.com/theos/sdks/raw/master/iPhoneOS14.5.sdk.tar.xz -o $HOME/theos/sdks/sdk.tar.xz
          tar -xf $HOME/theos/sdks/sdk.tar.xz -C $HOME/theos/sdks
          
          # Téléchargement des Headers Substrate
          curl -L https://github.com/theos/headers/archive/master.tar.gz | tar -xz -C $HOME/theos/include --strip-components=1

      - name: Build Package
        run: |
          export THEOS=$HOME/theos
          export PATH=$THEOS/bin:$PATH
          # On compile en mode rootless pour les iOS récents
          make package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless
        
      - name: Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: JoyBoy-Package-DEB
          path: packages/*.deb
